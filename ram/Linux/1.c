main(void)
{
   
  FILE *fp;
 
        fp=fopen(filename,"w");
  
       fprintf(fp,"print "\n".join(map(lambda x:");
       fprintf(fp,""%.6f %.6f %.6f %.6f %.6f %.6f" % tuple(x), molsystem.conf[molname]))");    
    
        
       fclose(fp);
}
